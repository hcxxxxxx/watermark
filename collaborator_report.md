# RelMel 语音水印工作汇报

本文档用于向合作者快速说明当前工作的基本情况、方法思路、实验设计和已完成结果。当前工作主要围绕生成语音水印展开，目标是在声码器生成语音中嵌入可验证的用户身份信息，并在压缩、重采样、滤波、回声和加性噪声等攻击后仍能恢复水印。

## 1. 方法概述

### 为什么选择梅尔域加水印

当前工作选择在 mel spectrogram（梅尔频谱）上加水印，而不是直接在 waveform（波形）上加水印，主要有四个原因。

第一，mel spectrogram（梅尔频谱）是现代语音生成系统中的关键中间表示。很多 TTS（文本转语音）系统和 vocoder（声码器）流程都可以抽象为：

```text
文本或语音特征 -> mel spectrogram（梅尔频谱） -> waveform（波形）
```

因此，在 mel spectrogram（梅尔频谱）上加水印，相当于在声码器生成波形之前就把用户身份信息嵌入到生成条件中。这样水印不是后处理贴上去的，而是参与了语音生成过程。

第二，mel spectrogram（梅尔频谱）更贴近人耳感知。梅尔频带本身就是按照人耳对频率的非线性感知设计的。相比直接修改 waveform（波形）采样点，在梅尔域中控制水印强度和频带位置，更容易把扰动放在人耳相对不敏感、同时语音能量较稳定的位置，从而兼顾音质和可检测性。

第三，梅尔域水印更适合和声码器配合。波形域 watermark（波形水印）通常是在最终音频上做后处理，它可能会被后续压缩、重采样、播放录制链路或重新合成过程破坏。梅尔域方法则可以把水印嵌入到声码器输入中，让声码器把水印结构自然合成为波形。这对生成语音场景尤其合适。

第四，梅尔域提供了更清晰的结构可解释性。我们可以明确知道水印作用在哪些时间帧、哪些频带、哪些局部关系上。RelMel 正是利用这一点：它不直接依赖波形采样点，而是利用局部梅尔频带之间的 relative relation（相对关系）来表达 bit（比特）。这种结构让方法更容易分析，也更容易设计针对加性噪声的鲁棒机制。

简而言之，选择梅尔域是因为它位于生成语音的关键中间层，既和人耳感知相关，又和声码器生成过程紧密耦合，同时比直接波形后处理更容易设计可解释的鲁棒水印结构。

### 任务目标

给定一段生成语音，希望在不明显损伤听感质量的前提下，嵌入一个用户身份 payload（载荷）。例如嵌入 32 bit（比特）的用户编号。之后即使音频经过常见后处理攻击，检测端仍能恢复该 payload（载荷），用于内容溯源和用户归因。

当前实验主要比较两类梅尔域水印方法：

- MelShield：已有 baseline（基线方法）。
- RelMel：当前提出的方法。

此外，还补充了两个公开 waveform-domain baseline（波形域基线）：

- AudioSeal。
- WavMark。

### MelShield 方法

MelShield 的基本思想是在 mel spectrogram（梅尔频谱）上叠加由 secret key（密钥）生成的 pseudo-random pattern（伪随机图案）。

对于每一个 bit（比特），MelShield 都生成一个对应的随机图案。如果该 bit（比特）为 1，就正向叠加该图案；如果为 0，就反向叠加该图案。最终水印可以理解为多个 bit（比特）对应图案的线性叠加。

识别时，检测端使用同一个 secret key（密钥）重新生成这些图案，然后在可疑音频的 mel residual（梅尔残差）里计算相关性。相关性为正判为 1，相关性为负判为 0。

该方法直观、容量容易扩展，但在强加性噪声下，残差中会混入较强噪声，随机图案的相关性会被污染，导致 bit accuracy（比特准确率）明显下降。

### RelMel 方法

RelMel 的核心思想是：不直接叠加随机二维图案，而是修改局部梅尔频带之间的 relative relation（相对关系）。

具体来说，RelMel 将 mel spectrogram（梅尔频谱）切成短时间 block（时间块）。在每个 block（时间块）中，方法用 secret key（密钥）选择两组梅尔频带：

- 正组频带。
- 负组频带。

如果要嵌入 bit 1，就让正组频带相对于负组频带更强；如果要嵌入 bit 0，就反过来。检测时不依赖绝对能量，而是看这两组频带之间的 residual difference（残差差值）方向。

当前实现中，一个 pair（频带对）不是单独两个频带，而是两组频带。例如 `pair_bins=6` 时，一个 bit（比特）对应：

- 6 个正组频带。
- 6 个负组频带。

检测分数可近似理解为：

```text
正组平均残差 - 负组平均残差
```

分数为正判为 1，分数为负判为 0。

### 可靠候选对选择

RelMel 的一个关键改进是 reliable candidate pair selection（可靠候选对选择）。对于每个 bit（比特）和每个 block（时间块），方法不是只随机选一个频带对，而是生成多个候选频带对，并选择最可靠的一个。

每个候选频带对的可靠性由两部分决定：

- 频带能量：能量较高的频带通常更稳定，不容易被噪声淹没。
- 边界余量：归一化 mel 值不能太接近 0 或 1，否则加扰动后容易被裁剪。

候选对打分时，会同时考虑正组和负组的可靠性，并惩罚两组可靠性不平衡的情况。直观上，RelMel 选择的是“两边都可靠、且两边可靠性比较均衡”的频带对。

这使得 RelMel 在加性噪声下比 MelShield 更稳。当前实验中，`pair_candidates`（候选对数量）从 1 增加到 16 后，强噪声下 bit accuracy（比特准确率）显著提升，而 clean PESQ（干净语音质量）只小幅下降。

### 与 baseline 的主要差异

| 对比项 | MelShield | RelMel |
|---|---|---|
| 水印载体 | 伪随机二维图案 | 局部梅尔频带相对关系 |
| bit（比特）表达方式 | 图案正负相关性 | 正负频带组的残差差值方向 |
| 检测方式 | 残差与伪随机图案做相关性 | 比较频带组相对变化并投票 |
| 对绝对幅值依赖 | 较强 | 较弱 |
| 对加性噪声鲁棒性 | 较容易下降 | 更稳定 |
| 核心改进点 | 扩频式随机图案 | 可靠候选频带对选择 |

## 2. 实验设计摘要

### 数据集与声码器

当前实验使用 LJSpeech 数据集，随机抽样，主要设置如下：

- 样本数：主要结果使用 random500。
- 随机种子：默认 seed 2026。
- 声码器：
  - HiFi-GAN。
  - DiffWave。

对于 DiffWave 实验，固定 DiffWave 采样 seed 为 0，以减少随机生成过程带来的波动。

### 方法与对照组

当前已完成或正在整理的对照包括：

| 类别 | 方法 | 说明 |
|---|---|---|
| 梅尔域 baseline（基线） | MelShield | 主要对照方法 |
| 当前方法 | RelMel | 本工作的核心方法 |
| 波形域 baseline（基线） | AudioSeal | 公开强 baseline，16 bit payload（载荷） |
| 波形域 baseline（基线） | WavMark | 公开强 baseline，16 bit payload（载荷） |

RelMel 和 MelShield 主实验使用 32 bit payload（载荷）。AudioSeal 和 WavMark 公开模型使用 16 bit payload（载荷），因此这两者更适合作为跨范式参考 baseline（基线），不应和 32 bit 梅尔域方法完全等价比较。

### 攻击设置

全攻击实验包含：

| 攻击名 | 含义 |
|---|---|
| none | 无攻击 |
| mp3 | MP3 有损压缩，128 kbps |
| aac | AAC 有损压缩，96 kbps |
| scale | 音量缩放，乘以 0.8 |
| rs16 | 重采样到 16 kHz |
| bandpass | 带通滤波，约 300 Hz 到 8000 Hz |
| lowpass | 低通滤波，截止频率 3000 Hz |
| noise20 | 添加 20 dB 信噪比高斯噪声 |
| noise10 | 添加 10 dB 信噪比高斯噪声 |
| noise5 | 添加 5 dB 信噪比高斯噪声 |
| echo | 添加 80 ms 延迟、0.35 衰减的回声 |

其中 `noise20/noise10/noise5` 是当前最关键的攻击组。数字越小，噪声越强。

### 主要评测指标

| 指标 | 含义 |
|---|---|
| bit_acc | bit accuracy（比特准确率），即恢复出的 bit（比特）有多少比例正确 |
| verification_rate | 验证率，即 bit_acc 超过阈值的音频比例 |
| PESQ_bm | 加水印或攻击后音频相对于无水印 benchmark（基准音频）的语音质量 |
| STOI_bm | 加水印或攻击后音频相对于 benchmark（基准音频）的可懂度 |
| confidence | 检测分数置信度，主要用于分析，不作为主表核心指标 |

论文主表建议重点报告：

- bit_acc。
- verification_rate。
- clean PESQ。
- attack PESQ / STOI。

其中 clean PESQ 主要用于约束水印引入的音质损伤。当前主要公平对比尽量让 RelMel 和 MelShield 的 clean PESQ 接近。

## 3. 实验数据表格

### 3.1 主对比结果

| 声码器 | 方法 | 来源 | 样本数 | clean PESQ | none | noise20 | noise10 | noise5 |
|---|---|---|---:|---:|---:|---:|---:|---:|
| HiFi-GAN | MelShield | 原文 | full |  | 1.0000 | 1.0000 | 0.7815 | 0.7052 |
| HiFi-GAN | MelShield | 本地复现 | 500 | 3.5069 | 1.0000 | 0.9072 | 0.7097 | 0.6391 |
| HiFi-GAN | RelMel | 当前方法 | 500 | 3.5102 | 0.9999 | 0.9946 | 0.9455 | 0.8648 |
| DiffWave | MelShield | 原文 | full |  | 1.0000 | 0.9834 | 0.7788 | 0.7006 |
| DiffWave | MelShield | 本地复现 | 500 | 3.5492 | 0.9998 | 0.9658 | 0.8212 | 0.7210 |
| DiffWave | RelMel | 当前方法 | 500 | 3.5443 | 0.9998 | 0.9914 | 0.9225 | 0.8308 |

DiffWave 质量匹配条件下，RelMel 和 MelShield 的 clean PESQ 非常接近：

```text
RelMel:     3.5443
MelShield:  3.5492
```

在该条件下，RelMel 相比 MelShield：

```text
noise20: +2.6 个百分点
noise10: +10.1 个百分点
noise5 : +11.0 个百分点
```

这说明 RelMel 的提升不是简单来自更大水印强度或更低音质，而是在相似音质下对加性噪声更稳。

### 3.2 DiffWave 全攻击质量匹配结果

| 攻击 | RelMel bit_acc | RelMel verification_rate | RelMel PESQ | MelShield bit_acc | MelShield verification_rate | MelShield PESQ |
|---|---:|---:|---:|---:|---:|---:|
| none | 0.9998 | 1.000 | 3.5443 | 0.9998 | 1.000 | 3.5492 |
| mp3 | 0.9998 | 1.000 | 3.5428 | 0.9997 | 1.000 | 3.5479 |
| aac | 0.9998 | 1.000 | 3.5191 | 0.9999 | 1.000 | 3.5338 |
| scale | 0.9998 | 1.000 | 3.5443 | 0.9998 | 1.000 | 3.5492 |
| rs16 | 0.9998 | 1.000 | 3.5443 | 0.9993 | 1.000 | 3.5492 |
| bandpass | 0.9998 | 1.000 | 3.5227 | 0.9991 | 1.000 | 3.5467 |
| lowpass | 0.9989 | 1.000 | 3.6133 | 0.9954 | 1.000 | 3.6115 |
| noise20 | 0.9914 | 1.000 | 1.4930 | 0.9658 | 1.000 | 1.4981 |
| noise10 | 0.9225 | 0.984 | 1.0787 | 0.8212 | 0.966 | 1.0788 |
| noise5 | 0.8308 | 0.852 | 1.0351 | 0.7210 | 0.872 | 1.0350 |
| echo | 0.9971 | 1.000 | 1.6116 | 0.9975 | 1.000 | 1.6060 |

非噪声攻击下，二者基本都接近满分。主要差异集中在加性噪声攻击，尤其是 `noise10` 和 `noise5`。

### 3.3 RelMel 可靠候选对消融

该实验验证 reliable candidate pair selection（可靠候选对选择）的作用。固定 HiFi-GAN、random500、`alpha=0.435`、`band=20:60`，只改变 `pair_candidates`（候选频带对数量）。

| pair_candidates | clean PESQ | none | noise20 | noise10 | noise5 |
|---:|---:|---:|---:|---:|---:|
| 1 | 3.5485 | 0.9999 | 0.9459 | 0.7674 | 0.6684 |
| 4 | 3.5299 | 0.9999 | 0.9873 | 0.8894 | 0.7845 |
| 8 | 3.5184 | 0.9999 | 0.9918 | 0.9232 | 0.8315 |
| 16 | 3.5102 | 0.9999 | 0.9952 | 0.9439 | 0.8607 |

从 `pair_candidates=1` 增加到 `pair_candidates=16` 后：

```text
noise20 提升约 4.9 个百分点
noise10 提升约 17.7 个百分点
noise5  提升约 19.2 个百分点
```

而 clean PESQ 仅从 3.5485 降到 3.5102。这说明可靠候选对选择是 RelMel 的主要有效机制之一。

### 3.4 多随机种子稳定性

固定 RelMel 主配置，使用不同随机种子抽取 random500 样本。

| seed | clean PESQ | none | noise20 | noise10 | noise5 |
|---:|---:|---:|---:|---:|---:|
| 2026 | 3.5102 | 0.9999 | 0.9952 | 0.9439 | 0.8607 |
| 2027 | 3.5108 | 0.9999 | 0.9950 | 0.9411 | 0.8588 |
| 2028 | 3.5164 | 0.9998 | 0.9938 | 0.9391 | 0.8574 |
| 平均 | 3.5125 | 0.9999 | 0.9946 | 0.9413 | 0.8590 |

结果表明主配置在不同随机抽样下比较稳定。

### 3.5 波形域 baseline 摘要

AudioSeal 和 WavMark 是 waveform-domain baseline（波形域基线），均使用 16 bit payload（载荷）。目前结果显示它们在非噪声攻击下很强，但在加性噪声下验证能力明显下降。

| 声码器 | 方法 | payload bits | clean PESQ | none ACC/VR | noise20 ACC/VR | noise10 ACC/VR | noise5 ACC/VR |
|---|---|---:|---:|---:|---:|---:|---:|
| HiFi-GAN | AudioSeal | 16 | 4.4574 | 1.0000 / 1.000 | 0.9736 / 0.304 | 0.7900 / 0.000 | 0.6465 / 0.000 |
| HiFi-GAN | WavMark | 16 | 4.2975 | 1.0000 / 1.000 | 待用 forced ACC 更新 | 待用 forced ACC 更新 | 待用 forced ACC 更新 |
| DiffWave | AudioSeal | 16 | 4.4225 | 1.0000 / 1.000 | 0.9659 / 0.138 | 0.7544 / 0.000 | 0.6175 / 0.000 |
| DiffWave | WavMark | 16 | 4.0204 | 1.0000 / 1.000 | 待用 forced ACC 更新 | 待用 forced ACC 更新 | 待用 forced ACC 更新 |

说明：WavMark 高层接口在强噪声下经常返回解码失败，因此原始统计会把失败记为 0，导致 ACC 看起来异常接近 0。当前已经修改脚本以输出 `mean_bit_acc_forced`，后续需要使用 forced decoding（强制解码）口径更新 WavMark 表格，使其更接近原文 ACC（准确率）统计方式。

## 当前结论

当前实验支持以下初步结论：

1. RelMel 在 HiFi-GAN 和 DiffWave 两种声码器上都能保持较高干净音质。
2. 在与 MelShield clean PESQ 基本匹配的情况下，RelMel 在加性噪声攻击下有明显优势。
3. RelMel 的核心机制 reliable candidate pair selection（可靠候选对选择）通过消融实验得到验证。
4. AudioSeal 和 WavMark 在非噪声攻击下很强，但在加性噪声下验证率下降明显，适合作为跨范式公开 baseline（基线）补充。
5. 后续还需要补充或完善：
   - WavMark forced decoding（强制解码）结果。
   - 假阳性实验，包括无水印、错误密钥、错误 payload（载荷）。
   - 更系统的 threshold（阈值）和 ROC（受试者工作特征曲线）分析。
