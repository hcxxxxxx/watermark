==================================================================
RAWMER: Reference-Assisted Speech Watermarking via
Relative Mel-Energy Relations
==================================================================
(Draft for ICASSP 2027 -- 4 pages text + 1 page references)


------------------------------------------------------------------
ABSTRACT
------------------------------------------------------------------
Neural text-to-speech and voice cloning create a pressing need to verify
whether a suspect utterance originates from a given generation service. We
target a reference-assisted setting in which the provider retains a compact
reference of the clean mel spectrogram at generation time and later checks
whether a suspect clip matches a claimed payload, key, and record. Within this
setting, prior mel-domain watermarking encodes the payload as an absolute
perturbation pattern whose residual is fragile under strong additive noise. We
propose RAWMER, which instead encodes each payload bit as a relative energy
relation between two reliable groups of mel bins within short time blocks, so
that detection relies on a differential score that cancels common-mode
distortion. A reliability-aware selection picks boundary-safe, energy-stable,
and balanced bin pairs from the clean reference, and the payload is embedded
redundantly across blocks for robust voting. On LJSpeech with HiFi-GAN and
DiffWave, RAWMER attains 0.9999/0.9998 bit accuracy on clean watermarked speech
(PESQ 3.51/3.51) and, at quality matched to a re-implemented MelShield baseline,
improves 5 dB-SNR accuracy from 0.6328 to 0.8587 under HiFi-GAN and from
0.7219 to 0.8353 under DiffWave. Negative
controls yield <= 0.4% false verification for wrong key/payload/reference,
references compress to about 22 KB per utterance, and 25% clips remain
verifiable under blind crop search. The main limitation is strong
time/frequency desynchronization such as large speed or pitch changes.


------------------------------------------------------------------
I. INTRODUCTION
------------------------------------------------------------------
Modern text-to-speech (TTS) and voice cloning systems synthesize speech that is
difficult to distinguish from genuine recordings, raising the risk of fraud,
impersonation, and misinformation and motivating mechanisms that trace audio
back to its source [REF: synthetic-speech-risk]. Audio watermarking, which
embeds an imperceptible payload that survives later processing, has become a
central tool for such provenance attribution [REF: audioseal, wavmark].

Most TTS systems map text to an intermediate mel spectrogram and then to a
waveform through a vocoder such as HiFi-GAN [REF: hifigan] or DiffWave
[REF: diffwave]. Because the mel spectrogram is a shared, model-agnostic
interface across these pipelines, recent work has moved keyed, in-generation
watermarking from the waveform into the mel domain: MelShield [REF: melshield]
treats the intermediate mel as the host signal and embeds a keyed payload via
low-energy spread-spectrum perturbations before vocoding, so that a provider who
retains the clean reference mel can later verify ownership without retraining the
vocoder. This mel-domain, reference-assisted design reports strong robustness
under common signal-processing distortions -- 99.98% bit accuracy under MP3/AAC
compression and 96.85% at 20 dB additive noise in our DiffWave
re-implementation -- while preserving
perceptual quality. We adopt the same setting: at generation time the provider
stores a compact clean-mel reference, and given a suspect clip it checks whether
the clip matches a claimed payload, key, and record.

Our point of departure is the encoding mechanism. MelShield, like prior
mel-domain schemes, represents each bit as an absolute perturbation pattern and
detects it by correlating the reference residual with the keyed pattern. Such
absolute residuals are sensitive to overall gain, frequency shaping, and vocoder
reconstruction error, and their separability degrades markedly under strong
distortion: at lower signal-to-noise ratios the embedded pattern is buried and
bit accuracy falls toward chance. This fragility under strong additive noise,
rather than under common compression or filtering, is the problem we target.

Our central observation is that, within a short time block, the relative energy
relation between two groups of mel bins is more stable than the absolute residual
of any single bin: gain changes, mild filtering, and local reconstruction bias
act in common on a local region and largely cancel in a differential measurement.
We propose RAWMER, which encodes each payload bit as the sign of an energy
difference between a positive and a negative group of mel bins, repeated across
time blocks, and a reliability-aware selection that uses the clean reference to
choose boundary-safe, energy-stable, and balanced bin pairs.

We make three contributions:

(1) Relative mel-energy watermarking. We formulate the payload as block-wise
relative energy relations between mel-bin groups rather than absolute
perturbation patterns, yielding a differential detection score robust to
common-mode distortion.

(2) Reliability-aware pair selection. We use the clean reference to pick
boundary-safe, energy-stable, and balanced positive/negative pairs from multiple
candidates, which we show is the main source of robustness under strong additive
noise.

(3) A systematic study of the setting. Across HiFi-GAN and DiffWave, RAWMER
improves additive-noise robustness over a quality-matched MelShield
re-implementation, while negative controls, references compressed to about 22 KB
per utterance, and blind partial-clip verification show the setting is sound and
practical. We also report a candid comparison with waveform-domain baselines and
disclose the main limitation, strong time/frequency desynchronization.


------------------------------------------------------------------
II. PROPOSED METHOD
------------------------------------------------------------------

A. Problem Setting: Reference-Assisted Verification
----------------------------------------------------
At generation time, the provider has access to a clean mel spectrogram
M in [0,1]^{F x T}, obtained by min-max normalizing the log-mel representation
of an utterance. For each utterance, a payload b in {0,1}^L is derived from the
utterance identifier and a secret key. The key remains in protected verifier
state; the retained record contains the utterance identifier, the payload or
metadata needed to recompute it, and a possibly compressed copy of M. RAWMER
embeds b into M to obtain a watermarked mel M_hat, which a vocoder G renders as
the released waveform x_hat = G(M_hat).

Verification is payload-specific rather than a binary presence test. Given a
suspect clip y, the verifier re-analyzes it into a mel spectrogram, normalizes it
with the stored reference statistics, aligns it to the reference within a small
frame shift, and forms

        R = M_y - M,                                                   (1)

where M_y is the aligned suspect mel. The detector decodes b_hat from R and
computes

        ACC = (1/L) sum_i 1[b_hat_i = b_i].                             (2)

The clip is verified if ACC >= tau. In all main experiments tau = 0.75. Thus
attribution depends on matching the claimed payload under the correct key and
reference, not on a confidence score alone. The negative controls in Section III
show that wrong keys, wrong payloads, and wrong references remain near chance.

B. Relative Mel-Energy Encoding
-------------------------------
RAWMER represents each bit by a relative energy relation rather than by an
absolute residual pattern. Embedding is restricted to a mel band
[f_lo, f_hi); in the main configuration this is bins 20-60 out of F = 80. The
time axis is divided into blocks of W frames with stride W, and each block
encodes a keyed subset of K payload bits. In our experiments W = 8 and K = 6.
The keyed subset rotates across blocks so that each payload bit is observed in
multiple blocks.

For bit i in block q, a keyed pseudo-random procedure selects two disjoint
groups of mel bins, P_{i,q} and N_{i,q}, each of size p. These groups define a
zero-sum pair vector v_{i,q} over the watermark band, with positive entries on
P_{i,q}, negative entries on N_{i,q}, and ||v_{i,q}||_2 = 1. A bit value is
encoded by the sign of the update: bit 1 increases P_{i,q} and decreases
N_{i,q}; bit 0 applies the opposite polarity. Updates are scaled by the
embedding strength alpha, an energy-dependent block weight, and 1/sqrt(K) so
that blocks carrying more active bits do not receive proportionally larger
energy. When several updates hit the same bin-frame location, the accumulated
perturbation is divided by a reuse factor. A boundary mask
clip(min(M,1-M)/delta,0,1), with margin delta, further prevents updates near the
normalization limits; the final watermarked mel is clipped to [0,1].

At detection, RAWMER subtracts the reference, removes the mean residual within
the watermark band, and computes a signed block score

        s_{i,q} = mean_t < R_q(:,t), v_{i,q} > .                         (3)

A positive score supports bit 1 and a negative score supports bit 0. Since
v_{i,q} is zero-sum, common-mode changes in the selected local band, such as
small gain shifts, mild spectral shaping, or smooth vocoder bias, are suppressed
by the differential measurement. This is the main contrast with absolute-pattern
watermarking, where detection relies on recovering a fixed residual pattern.

C. Reliability-Aware Pair Selection
-----------------------------------
Random bin pairs are not equally stable after vocoding. Near-silent bins may not
survive the waveform reconstruction, bins close to 0 or 1 have little headroom
for further perturbation, and an unbalanced positive/negative pair introduces a
biased differential score. RAWMER therefore ranks multiple candidate pairs using
the clean reference. For each bit/block, it samples C keyed candidate pairs
(C = 16 in the main configuration). For a bin f in the local clean block, we use
a reliability value

        rho(f) = mean_t m(f,t) [mu + (1-mu) M(f,t)^gamma],              (4)

where m(f,t) is the boundary mask, mu is the mask floor, and gamma controls the
energy emphasis. This couples boundary headroom and local speech energy: a bin
is reliable only if it can be perturbed and has enough energy to propagate
through the vocoder.

Let rho(P) and rho(N) be the average reliability of the positive and negative
groups. Candidate c is scored by

        S_c = min(rho(P), rho(N)) - 0.25 |rho(P) - rho(N)|.             (5)

The first term requires both sides to be reliable; the second penalizes
imbalanced pairs. The highest-scoring candidate is used for both embedding and
detection. Section III shows that increasing C from 1 to 16 substantially
improves robustness to additive noise with only a small clean-quality cost.

D. Block-Wise Embedding and Voting
----------------------------------
Each payload bit is repeated across the blocks selected for it, and detection
aggregates the corresponding signed scores. Specifically, each raw score
s_{i,q} is multiplied by the same clean-block energy factor and active-bit
normalization used at embedding. The bit-level score is then normalized by the
square root of the number of votes:

        S_i = (sum_q w_q a_q s_{i,q}) / sqrt(max(n_i,1)),
        b_hat_i = 1[S_i >= 0],                                          (6)

where n_i is the number of blocks that carry bit i, w_q is the clean-block
energy factor, and a_q = 1/sqrt(K_q) accounts for the number of active bits in
block q.

This redundancy turns local evidence into an utterance-level payload decision:
noise or cropping may corrupt some blocks, but remaining blocks still vote for
the correct bit. The design also supports partial-clip verification. If the
fragment offset is known, the verifier compares the fragment to the
corresponding reference span. If the offset is unknown, it performs a sliding
reference search and chooses the span with the strongest signed payload score;
the blind-fragment experiment in Section III uses a 4-frame search step.


------------------------------------------------------------------
III. EXPERIMENTS
------------------------------------------------------------------

A. Experimental Setup
---------------------
Dataset and vocoders. We evaluate on LJSpeech, embedding watermarks in the mel
domain and rendering waveforms with two vocoders, HiFi-GAN [REF: hifigan] and
DiffWave [REF: diffwave], so that robustness is measured across distinct
synthesis mechanisms. The main RAWMER/MelShield comparison uses 2000 random
utterances with seed 2026; waveform-domain baselines and ablations use 500
utterances unless stated otherwise. Mel spectrograms use F = 80 bins, and the
watermark band is bins 20-60.

Configuration. The payload is L = 32 bits and the verification threshold is
tau = 0.75. RAWMER uses blocks of W = 8 frames with stride 8, K = 6 bits per
block, group size p = 6, and C = 16 reliability-ranked candidates per bit.
The embedding strength is alpha = 0.435 for HiFi-GAN and alpha = 0.355 for
DiffWave, with mask floor 0.20 and boundary margin 0.01; the DiffWave sampler
uses a fixed seed.

Metrics. We report bit accuracy (ACC), the per-bit match between the decoded and
expected payload, and verification rate (VR), the fraction of clips with
ACC >= tau. Watermark imperceptibility is measured by PESQ and STOI of the
watermarked (or attacked) audio against an unwatermarked benchmark; we control
imperceptibility through the no-attack PESQ. Note that PESQ under strong
additive noise is low by construction, as the attack itself heavily degrades the
signal.

Baselines. MelShield [REF: melshield] is the closest baseline: mel-domain and
reference-assisted, but built on absolute perturbation patterns. We compare
against it in two ways. We cite the values reported in the MelShield paper as an
external reference, and we additionally re-implement and tune it to match 
RAWMER's clean PESQ, so that the two methods are compared
under one code base, vocoder, attack set, and metric. The reported and
re-implemented numbers are kept separate and never merged into a single
"fair" protocol. AudioSeal [REF: audioseal] and WavMark [REF: wavmark] are strong
waveform-domain baselines of a different paradigm; we report them separately
(Section III-C) and do not treat them as same-protocol comparisons, since they
embed 16-bit payloads on the waveform whereas the mel-domain methods embed
32 bits.

B. Robustness vs. MelShield
---------------------------
Table 1 reports the main comparison against MelShield across both vocoders. On
clean watermarked speech, RAWMER reaches ACC 0.9999 (HiFi-GAN, PESQ 3.51) and
0.9998 (DiffWave, PESQ 3.51) with VR 1.000, on par with MelShield's clean
accuracy. The difference appears under strong additive noise. For DiffWave, at
clean PESQ matched almost exactly (RAWMER 3.5140 vs. MelShield 3.5149), RAWMER
improves accuracy from 0.9685 to 0.9904 at 20 dB SNR, from 0.8225 to 0.9218 at
10 dB, and from 0.7219 to 0.8353 at 5 dB -- gains of 2.2, 9.9, and 11.3
percentage points. The HiFi-GAN comparison shows the same trend: at PESQ 3.51,
RAWMER attains 0.9942/0.9408/0.8587 at 20/10/5 dB, against a quality-matched
re-implementation at 0.9028/0.7129/0.6328 (PESQ 3.51). The MelShield paper's
reported figures, included in Table 1 as an external reference, follow a similar
pattern of degradation at 10 and 5 dB. Under non-noise attacks (MP3, AAC,
scaling, resampling, band-/low-pass, echo), both methods remain at or near VR
1.000, so the relative-energy formulation does not trade away common-attack
robustness to gain additive-noise robustness.

  TABLE 1. RAWMER vs. MelShield: clean PESQ and bit accuracy under additive
  noise (LJSpeech). "reported" = values from the MelShield paper (external
  reference, PESQ not directly matched); "re-impl." = our quality-matched
  re-implementation under the same protocol.

  Vocoder   Method                 Clean PESQ   none    noise20  noise10  noise5
  --------  ---------------------  ----------   ------  -------  -------  ------
  HiFi-GAN  MelShield (reported)        --      1.0000  1.0000   0.7815   0.7052
  HiFi-GAN  MelShield (re-impl.)      3.5100    0.9993  0.9028   0.7129   0.6328
  HiFi-GAN  RAWMER (ours)             3.5141    0.9999  0.9942   0.9408   0.8587
  DiffWave  MelShield (reported)        --      1.0000  0.9834   0.7788   0.7006
  DiffWave  MelShield (re-impl.)      3.5149    0.9998  0.9685   0.8225   0.7219
  DiffWave  RAWMER (ours)             3.5140    0.9998  0.9904   0.9218   0.8353

  All RAWMER/re-impl. rows: 2000 utterances; VR = 1.000 on clean speech.

C. Comparison with Waveform-Domain Baselines
--------------------------------------------
AudioSeal and WavMark belong to a different paradigm -- post-hoc waveform
watermarking with detector- or decoder-based recovery of a 16-bit payload -- so
Table 2 is a cross-paradigm reference rather than a same-protocol comparison.
Both baselines are very strong on non-noise attacks and introduce little clean
quality loss (clean PESQ above 4.0). Under strong additive noise, however, their
verification collapses. AudioSeal keeps high message bit accuracy at 20 dB
(0.9736 on HiFi-GAN) but its detector rejects most clips, giving VR 0.304
(HiFi-GAN) and 0.138 (DiffWave), and VR 0.000 at 10 and 5 dB. WavMark, reported
with forced decoding, falls to near-chance accuracy at 10/5 dB (0.5114/0.4926 on
HiFi-GAN), and its high-level decode rate is essentially zero under these noise
levels. Thus, within our reference-assisted setting and under strong additive
noise, RAWMER verifies far more reliably than these waveform baselines, while we
do not claim superiority on the non-noise attacks or clean quality where they
remain strong.

  TABLE 2. Waveform-domain baselines (16-bit payload) under additive noise:
  bit accuracy / verification rate. Cross-paradigm reference; 500 utterances.

  Vocoder   Method            Clean PESQ   noise20        noise10        noise5
  --------  ----------------  ----------   ------------   ------------   ------------
  HiFi-GAN  AudioSeal         4.4574       0.9736/0.304   0.7900/0.000   0.6465/0.000
  HiFi-GAN  WavMark (forced)  4.2975       0.7523/0.630   0.5114/0.046   0.4926/0.034
  DiffWave  AudioSeal         4.4225       0.9659/0.138   0.7544/0.000   0.6175/0.000
  DiffWave  WavMark (forced)  4.0211       0.6700/0.376   0.5120/0.034   0.4978/0.046

D. Validating the Reference-Assisted Setting
--------------------------------------------
A reference-assisted method must show that it does not pass for an arbitrary
reference, and that retaining references is practical. Table 3 summarizes four
studies on HiFi-GAN.

False positives. We run negative controls on 2000 utterances with no attack.
The correct combination of reference, key, and payload verifies at VR 1.000
(ACC 0.9999), whereas unmarked audio, a wrong key, a wrong payload, and a wrong
reference all drop to chance accuracy (ACC ~ 0.50) with VR of 0.40%, 0.15%,
0.40%, and 0.35%, respectively. Notably, the wrong-payload control retains a
confidence value equal to the positive case yet fails verification, confirming
that attribution must rest on payload match rather than signal strength alone.

Reference compression. Storing the full float32 mel costs 174.96 KB per
utterance. Quantizing only the watermark band (bins 20-60) to 8 bits reduces
this to 21.87 KB -- one eighth of the cost -- while preserving robustness:
ACC/VR of 0.9939/1.000, 0.9394/0.993, and 0.8584/0.925 at 20/10/5 dB,
essentially matching the float32 reference. A same-size full-mel 4-bit reference
drops more strongly, showing that preserving precision in the watermark band is
preferable to uniformly quantizing all mel bins. This makes per-utterance
reference storage realistic for a service provider.

Aggressive reference compression. We further compare RAWMER and MelShield under
more aggressive quantization on 500 utterances. RAWMER remains robust with only
the watermark band stored at 4 bits (11.05 KB): ACC/VR is 0.9826/1.000,
0.9128/0.984, and 0.8245/0.856 at 20/10/5 dB. At a comparable 11.05 KB,
MelShield's full-mel 2-bit reference gives 0.7859/0.982, 0.6554/0.710, and
0.6007/0.442, while MelShield band-only references degrade even more. This
suggests that, in our re-implementation, MelShield depends on a more complete
reference, whereas RAWMER's relation-based signal can be localized to the
watermark band and quantized more aggressively.

Blind partial-clip verification. To avoid assuming a known crop position, the
verifier slides each fragment over the reference (search step 4 frames) and
keeps the best-scoring alignment. A 25% fragment still verifies at ACC
0.985-0.991 with VR 0.998-1.000, and a 50% fragment at ACC >= 0.997 with VR
1.000; the recovered crop offset has a median error of about one mel frame.
Block-wise redundancy thus distributes watermark evidence evenly enough to
support short, unaligned clips.

  TABLE 3. Validating the reference-assisted setting on HiFi-GAN.
  (a) Negative controls, no attack, 2000 utterances (ACC / VR).
      correct          0.9999 / 1.0000
      clean unmarked   0.5008 / 0.0040
      wrong key        0.5030 / 0.0015
      wrong payload    0.5000 / 0.0040
      wrong reference  0.4991 / 0.0035
  (b) Reference compression, 2000 utterances (size; ACC/VR at noise5).
      float32          174.96 KB   0.8595 / 0.9305
      band_uint8        21.87 KB   0.8584 / 0.9245
      uint4             21.87 KB   0.8183 / 0.8415
  (c) Aggressive compression, 500 utterances (size; ACC/VR at noise10/noise5).
      RAWMER band_uint4       11.05 KB   0.9128/0.984   0.8245/0.856
      MelShield uint2         11.05 KB   0.6554/0.710   0.6007/0.442
      MelShield band_uint8    22.09 KB   0.5767/0.368   0.5498/0.236
  (d) Blind fragment verification, 500 utterances (ACC / VR).
      25% clip         0.985-0.991 / 0.998-1.000
      50% clip         >= 0.997    / 1.000

E. Ablation and Limitations
---------------------------
Pair selection. Table 4 isolates the effect of reliability-aware selection on
HiFi-GAN. Increasing the candidate count C from 1 (a single random pair) to 16
raises noise robustness sharply -- by 4.9, 17.7, and 19.2 points at 20/10/5 dB
-- while clean PESQ drops only from 3.5485 to 3.5102. Reliability-aware pair
selection is therefore the principal source of RAWMER's additive-noise
robustness, at negligible perceptual cost. Across seeds 2026-2028 the main
configuration is stable (noise5 ACC 0.8607/0.8588/0.8574).

  TABLE 4. Pair-candidate ablation on HiFi-GAN, 500 utterances.

  C    Clean PESQ   none    noise20  noise10  noise5
  ---  ----------   ------  -------  -------  ------
  1    3.5485       0.9999  0.9459   0.7674   0.6684
  4    3.5299       0.9999  0.9873   0.8894   0.7845
  8    3.5184       0.9999  0.9918   0.9232   0.8315
  16   3.5102       0.9999  0.9952   0.9439   0.8607

Robustness scope and limitations. Beyond the common attacks of Table 1, RAWMER
stays robust under modern operations: low-bitrate MP3/AAC, 2 kHz low-pass, wide
band-pass, amplitude clipping, 8-bit quantization, and strong reverberation all
keep VR at 1.000 on both vocoders. Neural codec compression shows the same
trend: under EnCodec at 6/12/24 kbps, RAWMER keeps ACC in the range 0.976-0.995
with VR >= 0.998 across the two vocoders, above MelShield's 0.888-0.976 in the
same random500 protocol. Two boundaries remain. Extreme 0 dB additive
noise lowers VR to 0.486-0.598. More importantly, RAWMER is sensitive to
time/frequency desynchronization: detection relies on frame-level alignment
between the suspect and reference mels, so large speed changes and pitch shifts
misalign the blocks and shift the chosen bin relations. In a HiFi-GAN sweep,
mild distortions are tolerated: pitch shifts within +/-50 cents keep ACC above
0.995 and VR 1.000, and +/-3% speed keeps VR above 0.99. Across the two vocoders,
however, a one-semitone (+/-100 cents) pitch shift reduces ACC to 0.65-0.79, and
+/-10% speed drops VR below 0.33. The dominant failure mode is not loss of
watermark energy but broken synchronization, which points to multi-scale
alignment, dynamic time warping, or pitch-aware matching as future work.


------------------------------------------------------------------
IV. CONCLUSION
------------------------------------------------------------------
We introduced RAWMER, a reference-assisted mel-domain watermarking method for
neural speech provenance verification. RAWMER replaces absolute residual
patterns with relative mel-energy relations between reliability-selected bin
groups, and decodes the payload by aggregating differential block scores against
a retained clean-mel reference. Experiments on LJSpeech with HiFi-GAN and
DiffWave show that this design improves robustness to strong additive noise over
a quality-matched MelShield re-implementation, while maintaining high
verification rates under common signal-processing and neural codec attacks.
Negative controls with wrong
keys, payloads, and references keep verification near chance; band-limited
reference quantization reduces storage to about 22 KB per utterance with little
loss; and blind fragment search verifies short, unaligned clips. These results
support the feasibility of reference-assisted mel-domain watermarking for
provider-side attribution. The main limitation is synchronization: large speed
changes and pitch shifts can misalign the reference and suspect mels. Future
work will therefore focus on multi-scale temporal alignment and pitch-aware
matching.


------------------------------------------------------------------
V. REFERENCES
------------------------------------------------------------------
NOTE: bracket tags used in the body map to the numbers below:
  [REF: synthetic-speech-risk] -> [11]/[12]   [REF: audioseal] -> [3]
  [REF: wavmark] -> [4]   [REF: hifigan] -> [5]   [REF: diffwave] -> [6]
  [REF: melshield] -> [1]   ([16],[20],[21] are unverified -- see chat notes)

[1]  Y. Jin, Q. Li, L. Liu, and J. Ni, "MelShield: Robust mel-domain audio
     watermarking for provenance attribution of AI-generated synthesized
     speech," arXiv preprint arXiv:2605.01515, 2026.

[2]  J. Zhou, J. Yi, T. Wang, J. Tao, Y. Bai, C. Y. Zhang, Y. Ren, and Z. Wen,
     "TraceableSpeech: Towards proactively traceable text-to-speech with
     watermarking," arXiv preprint arXiv:2406.04840, 2024.

[3]  R. San Roman, P. Fernandez, H. Elsahar, A. Defossez, T. Furon, and T. Tran,
     "Proactive detection of voice cloning with localized watermarking," in
     Proc. Int. Conf. Machine Learning (ICML), vol. 235, 2024.

[4]  G. Chen, Y. Wu, S. Liu, T. Liu, X. Du, and F. Wei, "WavMark: Watermarking
     for audio generation," arXiv preprint arXiv:2308.12770, 2023.

[5]  J. Kong, J. Kim, and J. Bae, "HiFi-GAN: Generative adversarial networks for
     efficient and high fidelity speech synthesis," in Proc. Advances in Neural
     Information Processing Systems (NeurIPS), vol. 33, 2020, pp. 17022-17033.

[6]  Z. Kong, W. Ping, J. Huang, K. Zhao, and B. Catanzaro, "DiffWave: A
     versatile diffusion model for audio synthesis," in Proc. Int. Conf.
     Learning Representations (ICLR), 2021.

[7]  K. Ito and L. Johnson, "The LJ Speech dataset,"
     https://keithito.com/LJ-Speech-Dataset/, 2017.

[8]  A. W. Rix, J. G. Beerends, M. P. Hollier, and A. P. Hekstra, "Perceptual
     evaluation of speech quality (PESQ) - a new method for speech quality
     assessment of telephone networks and codecs," in Proc. IEEE Int. Conf.
     Acoustics, Speech, and Signal Processing (ICASSP), vol. 2, 2001,
     pp. 749-752.

[9]  C. H. Taal, R. C. Hendriks, R. Heusdens, and J. Jensen, "A short-time
     objective intelligibility measure for time-frequency weighted noisy
     speech," in Proc. IEEE Int. Conf. Acoustics, Speech and Signal Processing
     (ICASSP), 2010, pp. 4214-4217.

[10] J. Shen, R. Pang, R. J. Weiss, M. Schuster, N. Jaitly, Z. Yang, Z. Chen,
     Y. Zhang, Y. Wang, R. Skerrv-Ryan, R. A. Saurous, Y. Agiomyrgiannakis, and
     Y. Wu, "Natural TTS synthesis by conditioning WaveNet on mel spectrogram
     predictions," in Proc. IEEE Int. Conf. Acoustics, Speech and Signal
     Processing (ICASSP), 2018, pp. 4779-4783.

[11] Z. Wu, E. S. Chng, and H. Li, "Detecting converted speech and natural
     speech for anti-spoofing attack in speaker recognition," in Proc.
     Interspeech, 2012.

[12] X. Liu, X. Wang, M. Sahidullah, J. Patino, H. Delgado, T. Kinnunen,
     M. Todisco, J. Yamagishi, N. Evans, A. Nautsch, and K. A. Lee, "ASVspoof
     2021: Towards spoofed and deepfake speech detection in the wild,"
     IEEE/ACM Trans. Audio, Speech, Language Process., vol. 31, pp. 2507-2522,
     2023.

[13] Y. Ren, C. Hu, X. Tan, T. Qin, S. Zhao, Z. Zhao, and T.-Y. Liu,
     "FastSpeech 2: Fast and high-quality end-to-end text to speech," in Proc.
     Int. Conf. Learning Representations (ICLR), 2021.

[14] J. Casanova, J. Weber, C. D. Shulby, A. C. Junior, E. Golge, and
     M. A. Ponti, "YourTTS: Towards zero-shot multi-speaker TTS and zero-shot
     voice conversion for everyone," in Proc. Int. Conf. Machine Learning
     (ICML), 2022, pp. 2709-2720.

[15] C. Liu, J. Zhang, T. Zhang, X. Yang, W. Zhang, and N. Yu, "Detecting voice
     cloning attacks via timbre watermarking," arXiv preprint arXiv:2312.03410,
     2023.

[16] G. Chen, Y. Wu, S. Liu, T. Liu, X. Du, and F. Wei, "Robust audio
     watermarking based on reversible networks," arXiv preprint, 2023.

[17] J. Zhou, J. Yi, Y. Ren, J. Tao, T. Wang, and C. Y. Zhang, "WMCodec:
     End-to-end neural speech codec with deep watermarking for authenticity
     verification," arXiv preprint arXiv:2409.12121, 2024.

[18] A. Defossez, J. Copet, G. Synnaeve, and Y. Adi, "High fidelity neural audio
     compression," arXiv preprint arXiv:2210.13438, 2022.

[19] N. Zeghidour, A. Luebs, A. Omran, J. Skoglund, and M. Tagliasacchi,
     "SoundStream: An end-to-end neural audio codec," IEEE/ACM Trans. Audio,
     Speech, Language Process., vol. 30, pp. 495-507, 2021.

[20] P. O'Reilly, A. Bugler, K. Bhandari, M. Morrison, and B. Pardo, "VoiceBlock:
     Privacy through real-time adversarial attacks with audio-to-audio models,"
     in Proc. Advances in Neural Information Processing Systems (NeurIPS), 2022.

[21] H. Liu, T. Guo, S. Jin, J. Liu, W. Zhao, and D. Yu, "Detecting and
     attributing AI-generated speech: A survey," arXiv preprint, 2024.
